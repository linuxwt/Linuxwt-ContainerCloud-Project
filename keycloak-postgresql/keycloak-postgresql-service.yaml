apiVersion: v1
kind: Service
metadata:
  name: keycloak-database
  namespace: devops
  labels:
    app: keycloak-database
spec:
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: keycloak-database