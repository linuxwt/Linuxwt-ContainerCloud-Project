apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: devops
  labels:
    app: keycloak
spec:
  type: NodePort
  ports:
    - name: keycloak
      port: 8080
      targetPort: 8080
      nodePort: 30007
  selector:
    app: keycloak
