# apiVersion: traefik.containo.us/v1alpha1
# kind: Middleware
# metadata:
  # name: redirect-https
  # namespace: devops
# spec:
  # redirectScheme:
    # scheme: https
    # port: "443"
    # permanent: true
# ---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak-http-route
  namespace: devops
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`keycloak.k8s`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: keycloak
          port: 8080
      middlewares:
        - name: redirect-https
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak-https-route
  namespace: devops
spec:
  entryPoints:
    - websecure
  tls:
    secretName: keycloak.k8s-tls
  routes:
    - match: Host(`keycloak.k8s`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: keycloak
          port: 8080